

	struct bug{
		char bugId[20];
		char bugName[20];
		char bugStatus[20];
		char bugReportingDate[20];
		char bugReporterId[20];
		char bugAssignedToDevId[20];
	};

	struct bug bugDetails[10];



char* id,name,date,status,testerId,devId;

fp=fopen("buglist.txt","r");
	while (fscanf(fp,"%s %s %s %s %s %s",&status, &id, &name, &date, &testerId, &devId) != EOF) 
	{
		strcpy(bugDetails[x].bugStatus,status);
		strcpy(bugDetails[x].bugId,id);
		strcpy(bugDetails[x].bugName,name);
		strcpy(bugDetails[x].bugReportingDate,date);
		strcpy(bugDetails[x].bugReporterId,testerId);
		strcpy(bugDetails[x].bugAssignedToDevId,devId);
		
		if(strcmp(bugDetails[x].bugReporterId,res)){
			printf("%s \t %s \t %s \t %s \t %s \t %s",bugDetails[x].bugStatus,bugDetails[x].bugId,bugDetails[x].bugName,bugDetails[x].bugReportingDate,bugDetails[x].bugReporterId,bugDetails[x].bugAssignedToDevId);
		}
		x++;

		
	}